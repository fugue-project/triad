name: Publish to PyPI

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    uses: fugue-project/.common/.github/workflows/pypi-publish/workflow.yml@setup/2
    secrets:
      PYPI_TOKEN: ${{ secrets.PYPI_TOKEN }}
